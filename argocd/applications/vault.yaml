# argocd/applications/vault.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "-5"  # Install early
spec:
  project: default
  source:
    repoURL: https://helm.releases.hashicorp.com
    chart: vault
    targetRevision: 0.31.0
    syncPolicy:
      applicationsSync: create-only
    helm:
      values: |
        global:
          enabled: true
          tlsDisable: false
        server:
          extraEnvironmentVars:
            VAULT_CACERT: /vault/userconfig/vault-ha-tls/ca.crt
          #  VAULT_TLSCERT: /vault/userconfig/vault-ha-tls/tls.crt
          #  VAULT_TLSKEY: /vault/userconfig/vault-ha-tls/tls.key
          volumes:
            - name: userconfig-vault-ha-tls
              secret:
                defaultMode: 420
                secretName: vault-ha-tls
          volumeMounts:
            - mountPath: /vault/userconfig/vault-ha-tls
              name: userconfig-vault-ha-tls
              readOnly: true

          standalone:
            enabled: true
          
          # HA mode (recommended for production)
          ha:
            replicas: 2
            raft:
              enabled: true
              setNodeId: true
              config: |
                 cluster_name = "vault-integrated-storage"
                 ui = true
                 listener "tcp" {
                    tls_disable = 0
                    address = "[::]:8200"
                    cluster_address = "[::]:8201"
                    tls_cert_file = "/vault/userconfig/vault-ha-tls/tls.crt"
                    tls_key_file  = "/vault/userconfig/vault-ha-tls/tls.key"
                    tls_client_ca_file = "/vault/userconfig/vault-ha-tls/ca.crt"
                 }
                 storage "raft" {
                    path = "/vault/data"
                 }
                 disable_mlock = true
                 service_registration "kubernetes" {}
          
          # Resources
          resources:
            requests:
              memory: 256Mi
              cpu: 250m
            limits:
              memory: 512Mi
              cpu: 500m
          
          # Ingress
          ingress:
            enabled: true
            ingressClassName: traefik
            annotations:
              traefik.ingress.kubernetes.io/service.serversscheme: https
              traefik.ingress.kubernetes.io/router.middlewares: vault-vault-server-headers@kubernetescrd
              traefik.ingress.kubernetes.io/service.serverstransport: vault-transport@kubernetescrd  # Use the transport
              traefik.ingress.kubernetes.io/router.entrypoints: websecure
              traefik.ingress.kubernetes.io/router.tls: "true"
              cert-manager.io/cluster-issuer: selfsigned-issuer
            hosts:
              - host: vault.example.com
                paths:
                  - /
            tls:
              - secretName: vault-ui-tls
                hosts:
                  - vault.example.com
          
          # Data storage
          dataStorage:
            enabled: true
            size: 10Gi
            storageClass: nfs-client
          
          # Audit storage
          auditStorage:
            enabled: true
            size: 10Gi
            storageClass: nfs-client
        
        ui:
          enabled: true
          serviceType: ClusterIP
        
        # Injector (for automatic secret injection)
        injector:
          enabled: true
          certs:
            secretName: vault-ha-tls
  destination:
    server: https://kubernetes.default.svc
    namespace: vault
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
